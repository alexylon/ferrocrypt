//! General path helpers — encrypted filename derivation, base-name
//! extraction, and user-path error mapping.
//!
//! Populated in step 3 of the restructure plan. Currently empty so the
//! module declaration in [`crate::fs`] resolves.
