[workspace]
resolver = "2"

members = [
    "ferrocrypt-lib",
    "ferrocrypt-cli",
    "ferrocrypt-test-support",
]

exclude = [
    "experiments",
    "ferrocrypt-desktop",
    "ferrocrypt-lib/fuzz",
]
