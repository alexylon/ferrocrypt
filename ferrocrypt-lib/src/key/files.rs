//! Filesystem-level key helpers — default filenames, key-file
//! classification, read/write wrappers, staging generated key files.
//!
//! Populated in step 5 of the restructure plan. Currently empty so
//! the module declaration in [`crate::key`] resolves.
